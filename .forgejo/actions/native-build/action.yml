inputs:
  version:
    description: "version to use"
    required: true
    type: string
runs:
  using: "composite"
  steps:
    - name: Native - checkstyle, build
      shell: bash
      run: |
        make clean dependencies checkstyle build