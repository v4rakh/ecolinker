inputs:
  version:
    description: "version to use"
    required: true
    type: string
runs:
  using: "composite"
  steps:
    - name: Native - test
      shell: bash
      run: |
        make test-coverage